arr = [10, 20, 30, 40]
|> str(arr@0)
|> str(#arr)

arr << [50]
|> str(arr)

sub = arr::1::3
|> str(sub)

combined = [1, 2] ++ [3, 4]
|> str(combined)

has = arr ?? [20]
|> str(has)

uniq = ~[1, 2, 2, 3, 1]
|> str(uniq)
